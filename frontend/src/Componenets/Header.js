import React from 'react'

export default function Header({ Role }) {

    // In case of Admin, we will return Admin console in header apart from other
    // In case of Mall Managers, we will return Manager console in the header apart from others
    // In case of Shop Owners, we will return Owner Console in the header apart from other
    // In case of customer nothing extra will be there.
    return (
        <div>

        </div>
    )
}
