# Shopping Mall Management System

Directions for running

1. Go to backend directory in command line and run

   `npm run start`

This will start the backend service

2. Go to the frontend directory in command line and run

   `npm start`

This will start the React local deployment server
